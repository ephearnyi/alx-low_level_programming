#include <stdio.h>

/**
 * main - prints numbers in base 10
 * Return: (0) successful
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf('\n');
	return (0);
}
